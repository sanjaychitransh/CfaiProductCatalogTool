# Production Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
rapidfuzz>=3.5.0
pydantic>=2.5.0
pyahocorasick>=2.0.0
rank-bm25>=0.2.2
python-dotenv>=1.0.0
ibmcloudant>=0.0.42

# Semantic Re-Ranking (Sentence-BERT + Cross-Encoder)
# Set USE_SBERT_RERANKER=true to activate; models are downloaded on first run.
sentence-transformers>=2.6.0

# LLM Reranking (optional — install whichever backend you use)
# watsonx.ai backend
ibm-watsonx-ai>=1.0.0
# OpenAI backend (fallback)
openai>=1.30.0

# Testing Dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Code Quality Tools (Optional - for development)
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
isort>=5.12.0

# Development Tools (Optional)
ipython>=8.14.0
ipdb>=0.13.13

# Made with Bob