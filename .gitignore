__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.env
.vscode/
.idea/
*.log

# Private keys — never commit
cfaiRS256.key
cfaiRS256.key.pub

# Local data (large / sensitive) — never commit
data/

# Local virtual environment — never commit
env/

# Bob agent workspace -- never commit
.bob/

# Playwright MCP session state -- never commit
.playwright-mcp/

# Tests folder - never commit
tests/
